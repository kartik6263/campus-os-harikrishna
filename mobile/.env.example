# The deployed backend. Delete this line (or comment it out) to use a
# backend running on this computer instead.
EXPO_PUBLIC_API_URL=https://campus-os-harikrishna-1.onrender.com

# Multi-institute mode: set to the control plane's URL and the app asks for an
# institute code on first launch, then talks to that institute's own backend.
# EXPO_PUBLIC_CONTROL_PLANE_URL=https://resolion-control-plane.onrender.com
